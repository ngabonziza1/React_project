# TODO

- [x] Update frontend `App.jsx` so that after successful registration it shows the **Login** screen (does not set `user` from registration).
- [ ] (If needed) Adjust backend session clearing or add an API route. (Will verify after frontend change.)
- [ ] Run backend + frontend and test: register a new account -> should show Login form (after rebuilding).

